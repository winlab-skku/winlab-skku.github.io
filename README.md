# Wireless Intelligence Lab (WIn Lab) — website

Static site served by GitHub Pages. These files are generated; edit them with the lab's admin tool
(the `winlab-website` working folder, kept off GitHub) and re-upload the changed files.
